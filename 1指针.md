# 1指针

## 1定义

### 1初始化

```c
int* p = &a	//int类型指针在64位操作系统中占8个字节，跟类型相关1
```

### 1 ‘* ’ 的定义

1、定义时*表后面变量是指针
2取值：~p后面表示取指针指向的内存空间的值

## 1计算

```c
int* p,char* q;
printf("%p\n",p+1)；	//int型位置+4
printf("%p\n",p+1)； //char型位置+1
```

### 1交换函数中形参的值，必须传地址

```c
void swap(int a,int b){
    int t = a;
    a = b;
    b = t;
}	//此函数对于主函数中的实参不改变值
```

### 1指针可赋值

```c
int a,*pa = &a,*pb;
pb = pa;
```

### 1

```c
++*px;	//与++(*px)含义相同
*px++;	//与(*px)++含义不同
		//*px++ 对位置进行运算，(*px)++ 对值进行运算
```

### 1

```c
const int* p1 = &num; 	// 常量指针 const修饰*p1 即 num
int const* p2 = &num;	// 指针常量 const修饰 p2 位置不能改变
```

