# 1指针

## 1定义

### 1初始化

```c
int* p = &a	//int类型指针在64位操作系统中占8个字节，跟类型相关1
```

### 1 ‘* ’ 的定义

1、定义时*表后面变量是指针
2取值：~p后面表示取指针指向的内存空间的值

## 1计算

```c
int* p,char* q;
printf("%p\n",p+1)；	//int型位置+4
printf("%p\n",p+1)； //char型位置+1
```

### 1交换函数中形参的值，必须传地址

```c
void swap(int a,int b){
    int t = a;
    a = b;
    b = t;
}	//此函数对于主函数中的实参不改变值
```

### 1指针可赋值

```c
int a,*pa = &a,*pb;
pb = pa;
```

### 1

```c
++*px;	//与++(*px)含义相同
*px++;	//与(*px)++含义不同
		//*px++ 对位置进行运算，(*px)++ 对值进行运算
```

### 1

```c
const int* p1 = &num; 	// 常量指针 const修饰*p1 即 num
int const* p2 = &num;	// 指针常量 const修饰 p2 位置不能改变
```

### 1结构体基础--学生信息

```C
#include<stdio.h>
#include <stdlib.h>	//malloc

typedef struct 
{
	char name[20];	//传入一个字符传用char，多个字符传用char*
	int id;
	double score;
}Student;

int main()
{
	int n,sum=0;
	printf("请输入学生数量");
	scanf_s("%d", &n);

	int new_n = n;


	Student* arr = (Student*)malloc(new_n * sizeof(Student));
	printf("请输入学生的学号 姓名 成绩\n");
	for (int i = 0; i < n; i++) { 
		scanf_s("%d", &arr[i].id);
		scanf_s("%s",arr[i].name, (unsigned)20);	//char输入需要写缓冲
		scanf_s("%lf", &arr[i].score);
		//scanf_s("%c%d%c", arr[i].id, arr[i].id, arr[i].name);scanf_s读取字符串必须额外加缓冲区长度
		sum += arr[i].score;
	}

	printf("学生信息\n");
	 for(int i = 0; i < n; i++) {
		 printf("学号%d 姓名%s 成绩%f", arr[i].id, arr[i].name, arr[i].score);	//char类型，输出格式%s
		//print("%d ", arr[i]);结构体不能直接打印
	}
	free(arr);
	return 0;
}
```

### 1交换

```C
#include<stdio.h>
void swap(int* a, int* b)
{
	int t = *a;
	*a = *b;
	*b = t;
}
int main()
{
	int a = 1, b = 2;
	swap(&a, &b);
	printf("a = %d, = %d", a, b);
	return 0;
}
```

### 1简单链表实现

```c
#include<stdio.h>
#include<stdlib.h>
typedef struct Node
{
	int data;
	struct Node* p = NULL;
}Node;	//节点

//头插法
void create(int value,Node** head)
{
	Node* node = (Node*)malloc(sizeof(Node));
	node->data = value;
	node->p = NULL;
	node = *head;
}

//删除头节点
void delet(Node**head)
{
	Node* temp;
	temp = *head;
	*head = (*head)->p;
	free(temp);
}
int main()
{
	Node* head;
	create(10,&head);
	create(20,&head);
	create(30,&head);
	delet(&head);
	printf("666");
	return 0;
}
```

