GPIO与LED流水灯学习电子笔记

## 1、GPIO基础（3-1）

1. GPIO核心定位

GPIO（通用输入/输出端口）是单片机核心外设接口，支持引脚双向输入/输出控制，是单片机与外设交互的基础单元。

2. GPIO的8种输入输出模式

- 输出模式：可控制端口输出高低电平，核心用于驱动LED、控制继电器等执行类外设
- 输入模式：读取端口的高低电平/电压，核心用于按键输入检测、外接模块电平信号采集、ADC电压采集、模拟通信协议数据接收等场景

## 1、LED流水灯开发（3-2）

### 1单片机外设使用通用三步

1. 时钟配置：RCC外设时钟，开启目标外设的工作时钟
2. 引脚初始化：调用 GPIO_Init 函数，配置GPIO引脚的工作模式、输出速度等核心参数
3. 引脚读写控制：通过输入/输出专用函数，控制GPIO引脚电平状态，实现外设的读写操作GPIO电平控制核心函数

函数名 功能说明 

| SetBits   | 将目标GPIO端口设置为高电平 |
| --------- | -------------------------- |
| ResetBits | 将目标GPIO端口设置为低电平 |

SetBits 将目标GPIO端口设置为高电平 
ResetBits 将目标GPIO端口设置为低电平 

### 1GPIO工作模式全解

| 模式名称    | 模式功能说明 |
| ----------- | ------------ |
| AIN         | 模拟输入模式 |
| IN FLOATING | 浮空输入模式 |
| IPD         | 下拉输入模式 |
| IPU         | 上拉输入模式 |
| OUT_OD      | 开漏输出模式 |
| OUT_PP      | 推挽输出模式 |
| AF_OD       | 复用开漏模式 |
| AF_PP       | 复用推挽模式 |

4. LED点亮原理（推挽输出模式）

LED的亮灭状态，由GPIO输出电平、LED正负极接线方式共同决定：

- 若LED正极接GPIO引脚：GPIO输出高电平时LED点亮，输出低电平时LED熄灭
- 若LED负极接GPIO引脚：GPIO输出低电平时LED点亮，输出高电平时LED熄灭

## 1AD/DA、数模、模数转换笔记（16‑1）

### 1AD（模数转换）

模拟‑数字转换，将模拟信号转换为单片机可识别的数字信号。

- 模拟信号：连续变化信号，连续变化的电压。
- 数字信号：只有两种状态，高电平、低电平（0、1）。

### 1AD模块框图

外部模拟信号输入 → AD转换器 → 单片机，输出数字量。
<img src="D:\typore\2026.9.14 C\stm32.assets\image-20260919060442462.png" alt="image-20260919060442462" style="zoom: 67%;" />

### 1运算放大器（运放）

运放是具有很大放大倍数的电路单元

- 内部集成差分电压功率放大器
- 放大倍数由外部负反馈系数决定

<img src="D:\typore\2026.9.14 C\stm32.assets\image-20260919060715176.png" alt="image-20260919060715176" style="zoom:67%;" />

### 1补充

1. 模数转换AD：模拟量 → 数字量（单片机可读）
2. 数模转换DA：数字量 → 模拟量（单片机输出数字转为电压）
